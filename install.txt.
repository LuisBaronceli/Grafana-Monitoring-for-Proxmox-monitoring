create a metric server called promox for the influx db and set the it to the container ip

restart all services
log into grafana
set up the data souce
import the a dashboard JSON
edit the dashboard to use the correct data source.

-------------------------------

Make sure you can ssh to your container.

ansible-playbook playbook.yml --private-key=/path/to/your/private/key